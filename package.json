{
  "name": "stottemedlem",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "story": "pnpm run stories",
    "stories": "turbo run build --filter=@stottemedlem/core --filter=@stottemedlem/db && pnpm --filter @stottemedlem/ui run storybook",
    "typecheck": "turbo run typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "nuke": "find . -maxdepth 3 -type d \\( -name node_modules -o -name .next -o -name .turbo -o -name dist \\) -not -path '*/node_modules/*' -exec rm -rf {} + && pnpm install"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "turbo": "^2.9.14",
    "typescript": "catalog:"
  }
}
