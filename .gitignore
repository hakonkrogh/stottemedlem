node_modules/
dist/
*.tsbuildinfo
.DS_Store
tmp/
.env
.env.local
# Cloudflare local secrets (real values); the .example template stays tracked.
.dev.vars
.dev.vars.*
!.dev.vars.example
.turbo/
.astro/
.wrangler/
worker-configuration.d.ts
storybook-static/
*storybook.log
# Vipps local test rig: the quick-tunnel origin and the harness' saved ids
# (agreement/charge ids and the webhook secret Vipps shows only once).
.vipps-tunnel
.vipps-test-state.json
